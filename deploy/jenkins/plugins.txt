# Configuration as Code
configuration-as-code

# Git & GitHub
git
github
github-branch-source

# Branch API — required for multibranch ‘Scan Now’ UI action
branch-api

# Folder & Multibranch — core plugins for the scan sidebar
cloudbees-folder
workflow-multibranch

# Pipeline
workflow-aggregator
pipeline-stage-view
pipeline-github-lib

# UI
blueocean

# Notifications — provides the office365ConnectorSend step used by the deploy
# pipelines to post build status to the Teams "Deployment - Jenkins_GA" channel
# (webhook comes from the "teams-jenkins-ga-webhook" credential in
# casc/jenkins.yaml). Plugin ids are case-sensitive: this one is capitalised.
Office-365-Connector

# Credentials
credentials
credentials-binding
ssh-credentials
plain-credentials

# SSH for deployments
ssh-agent
ssh-slaves

# Docker
docker-workflow
docker-plugin

# Utility
ws-cleanup
timestamper
build-timeout
ansicolor

# Job DSL (optional, for defining jobs as code)
job-dsl
