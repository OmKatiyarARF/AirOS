services:
  jenkins:
    image: jenkins-custom
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jenkins
    restart: unless-stopped
    # J1: no Docker socket is mounted. Jenkins cannot drive the host Docker
    # daemon directly — every deploy SSHes to its target and runs `docker
    # compose` there. The host runs Docker; Jenkins only orchestrates.
    ports:
      - "9080:8080"      # Jenkins UI (direct access, proxied via nginx on 90)
      - "50000:50000"    # Jenkins agent port
    volumes:
      - jenkins_home:/var/jenkins_home
      - ./casc:/var/jenkins_home/casc:ro
      - ./repos.json:/var/jenkins_home/repos.json:ro
      - ./pipelines:/var/jenkins_home/pipelines:ro
      - ${REPOS_VOLUME:-/home/ec2-user}:/home/ec2-user:ro
      # Deploy targets are no longer mounted writable into Jenkins: deploys run
      # over SSH on the host, which already owns those checkouts natively.
      # dss-* / hudd / erpnext / hrms deploy via SSH to remote hosts — no local mounts needed.
      # SECURITY: this used to point straight at the host's ~/.ssh (permanent
      # plaintext keys). It now points at ./secrets-runtime, a folder that
      # ../../security/fetch-secrets.sh regenerates fresh from AWS Secrets
      # Manager every time Jenkins starts (see start.sh). Nothing sensitive is
      # kept on disk here between restarts.
      - ${SSH_VOLUME:-./secrets-runtime}:/var/jenkins_home/.ssh:ro
    env_file:
      # GITHUB_TOKEN lives here now (fetched from AWS Secrets Manager by
      # fetch-secrets.sh), not as a plaintext line in .env anymore.
      - .env.secrets
    environment:
      - CASC_JENKINS_CONFIG=/var/jenkins_home/casc/jenkins.yaml
      - JENKINS_ADMIN_USER=${JENKINS_ADMIN_USER}
      - JENKINS_ADMIN_PASSWORD=${JENKINS_ADMIN_PASSWORD}
      - GITHUB_USER=${GITHUB_USER}
      # Second GitHub account/org (optional) — referenced by casc id "github-airawat"
      - GITHUB_AIRAWAT_USER=${GITHUB_AIRAWAT_USER:-}
      - REPO_NAME=${REPO_NAME:-}
      - DEPLOY_PATH=${DEPLOY_PATH:-}
      - DEPLOY_TYPE=${DEPLOY_TYPE:-}
      # Enable Content Security Policy (fixes dashboard warning)
      - JAVA_OPTS=-Djenkins.install.runSetupWizard=false -Djenkins.security.ContentSecurityPolicy.policy="default-src 'self'; script-src 'self'; style-src 'self';"
    networks:
      - jenkins-net
      - ec2_default   # lets Kong (on the ec2_default network) reach Jenkins for jenkins.airawat.org
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/login"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  nginx:
    image: nginx:alpine
    container_name: jenkins-nginx
    restart: unless-stopped
    ports:
      - "90:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      jenkins:
        condition: service_healthy
    networks:
      - jenkins-net

volumes:
  jenkins_home:

networks:
  jenkins-net:
    driver: bridge
  ec2_default:
    external: true
